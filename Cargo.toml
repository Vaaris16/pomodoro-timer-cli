[package]
name = "pomodoro-timer-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
colored = "3.1.1"
crossterm = "0.29.0"
figlet-rs = "1.0.0"
rodio = { version = "0.22.2", default-features = false, features = ["mp3", "playback"] }
